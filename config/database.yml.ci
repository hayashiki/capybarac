default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

test:
  <<: *default
  database: capybarac_test
  host: localhost
  username: postgres
  port: <%= ENV.fetch('DB_PORT', '45432') %>